pre-commit:
  commands:
    fmt:
      run: alejandra {staged_files}
      glob: "*.nix"
      stage_fixed: true

pre-push:
  commands:
    dry-build:
      run: nh os build . --hostname carbon --no-nom --quiet -- --quiet
