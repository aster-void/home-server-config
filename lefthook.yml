pre-commit:
  commands:
    fmt:
      run: alejandra {staged_files}
      glob: "*.nix"
      stage_fixed: true

pre-push:
  commands:
    dry-build:
      run: set -o pipefail && nh os build . --hostname carbon --no-nom --quiet 2>&1 | tail -n 30
